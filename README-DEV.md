## Router

We use Expo Router (file-based routing). Babel includes `expo-router/babel` to ensure consistent routing behavior across Web and native.
Checklist Universal

TypeScript: npm run typecheck

Web responsiva: npx expo start --clear → w

Móvil: npx expo start --clear → a o i
