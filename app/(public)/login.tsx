import { Text, View } from 'react-native';

export default function LoginRoute() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-base">Login (placeholder)</Text>
    </View>
  );
}
