import { Text, View } from 'react-native';

export default function RegisterRoute() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-base">Register (placeholder)</Text>
    </View>
  );
}
